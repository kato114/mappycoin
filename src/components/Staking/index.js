import FadeIn from "react-fade-in/lib/FadeIn"

import { GreenCircle } from "resources/SVGs"
import "./style.scss"

const Staking = () => (
  <div className="staking flex flex-column">
    <GreenCircle className="staking-decoration-navbar" />
  </div>
)

export default Staking
